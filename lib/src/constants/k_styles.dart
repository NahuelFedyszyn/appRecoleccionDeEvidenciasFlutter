import 'package:flutter/material.dart';

inputTextDecoration(String label) => InputDecoration(
      labelText: label,
      labelStyle: TextStyle(
        color: Colors.white,
      ),
    );

inputTextStyleButtons() => TextStyle(
      color: Colors.white,
    );
